<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/car-management.css">
    <script src="/js/confirmDelete.js"></script>
</head>
<body>
    <div class="d-flex">
        <%- include('partials/admin-sidebar', { currentPath: '/carManagement' }) %>
        <main class="container">
            <div class="search-and-register mb-4">
                <form action="/carManagement" method="GET" class="search-form">
                    <input type="text" class="search-input form-control mb-2" name="search" placeholder="車種名で検索" value="<%= searchTerm %>">
                    
                    <select name="manufacturer" class="form-select mb-2">
                        <option value="">メーカー名を選択</option>
                        <% manufacturers.forEach(manufacturer => { %>
                            <option value="<%= manufacturer.manufacturer_id %>" <%= manufacturerId == manufacturer.manufacturer_id ? 'selected' : '' %>><%= manufacturer.manufacturer_name %></option>
                        <% }) %>
                    </select>

                    <div class="mb-2">
                        <label for="car_year">年式:</label>
                        <input type="number" id="car_year" name="car_year" class="form-control" placeholder="年式" value="<%= carYear %>">
                    </div>

                    <div class="mb-2">
                        <label for="min_mileage">走行距離（最小）:</label>
                        <input type="number" id="min_mileage" name="min_mileage" class="form-control" placeholder="最小距離" value="<%= minMileage %>">
                    </div>
                    <div class="mb-2">
                        <label for="max_mileage">走行距離（最大）:</label>
                        <input type="number" id="max_mileage" name="max_mileage" class="form-control" placeholder="最大距離" value="<%= maxMileage %>">
                    </div>

                    <select name="car_status" class="form-select mb-2">
                        <option value="在庫あり" <%= carStatus === '在庫あり' ? 'selected' : '' %>>在庫あり</option>
                        <option value="出品中" <%= carStatus === '出品中' ? 'selected' : '' %>>出品中</option>
                        <option value="落札済み" <%= carStatus === '落札済み' ? 'selected' : '' %>>落札済み</option>
                    </select>

                    <button type="submit" class="btn btn-primary">検索</button>
                </form>
                <a href="/carRegistration" class="register-button btn btn-success">車両を登録する</a>
            </div>

            <section class="car-list">
                <h5>検索結果: <%= cars.length %> 件</h5>
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>車両ID</th>
                            <th>車両画像</th>
                            <th>車種</th>
                            <th>メーカー名</th>
                            <th>年式</th>
                            <th>走行距離</th>
                            <th>車体カラー</th>
                            <th>状態</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (cars.length === 0) { %>
                            <tr>
                                <td colspan="9" class="text-center">車両データがありません</td>
                            </tr>
                        <% } else { %>
                            <% cars.forEach(car => { %>
                                <tr>
                                    <td><%= car.car_id %></td>
                                    <td class="image-cell">
                                        <% if (car.car_image) { %>
                                            <img src="<%= car.car_image %>" alt="車両画像" class="car-image" style="width: 100px;">
                                        <% } else { %>
                                            <p>画像なし</p>
                                        <% } %>
                                    </td>
                                    <td><%= car.car_type %></td>
                                    <td><%= car.manufacturer_name %></td>
                                    <td><%= car.car_year %></td>
                                    <td><%= car.car_mileage %></td>
                                    <td><%= car.car_color %></td>
                                    <td><%= car.car_status %></td>
                                    <td>
                                        <div class="action-buttons">
                                            <a href="/editCar/<%= car.car_id %>" class="btn btn-warning">編集</a>
                                            <form action="/deleteCar/<%= car.car_id %>" method="POST" onsubmit="return confirmDelete(event, '<%= car.car_id %>')" style="display:inline;">
                                                <button type="submit" class="btn btn-danger">削除</button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>
                </table>
            </section>

            <div class="pagination">
                <% if (totalPages > 1) { %>
                    <nav aria-label="Page navigation">
                        <ul class="pagination">
                            <% for (let i = 1; i <= totalPages; i++) { %>
                                <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                                    <a class="page-link" href="/carManagement?page=<%= i %>"><%= i %></a>
                                </li>
                            <% } %>
                        </ul>
                    </nav>
                <% } %>
            </div>
        </main>
    </div>
</body>
</html>