<!DOCTYPE html>
<html lang="html">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/car-management.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
    <script src="/js/confirmDelete.js"></script>
</head>
<body>
    <%- include('partials/admin-header') %>
    <main class="container">
        <!-- 検索フォームと出品登録ボタン -->
        <div class="search-and-register">
            <form action="/carManagement" method="GET" class="search-form">
                <input type="text" class="search-input" name="search" placeholder="車種名で検索">
                <button type="submit" class="search-button">検索</button>
            </form>
            <a href="/carRegistration" class="register-button">車両を登録する</a>
        </div>

        <section class="car-list">
            <table class="car-table">
                <thead>
                    <tr>
                        <th>車両ID</th>
                        <th>車両画像</th>
                        <th>車種</th>
                        <th>メーカー名</th>
                        <th>年式</th>
                        <th>走行距離</th>
                        <th>車体カラー</th>
                        <th>状態</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (cars.length === 0) { %>
                        <tr>
                            <td colspan="9" class="no-data">車両データがありません</td>
                        </tr>
                    <% } else { %>
                        <% cars.forEach(car => { %>
                            <tr>
                                <td><%= car.car_id %></td>
                                <td class="image-cell">
                                    <% if (car.car_image) { %>
                                        <img src="<%= car.car_image %>" alt="車両画像" class="car-image">
                                    <% } else { %>
                                        <p>画像なし</p>
                                    <% } %>
                                </td>
                                <td><%= car.car_type %></td>
                                <td><%= car.car_manufacturer %></td>
                                <td><%= car.car_year %></td>
                                <td><%= car.car_mileage %></td>
                                <td><%= car.car_color %></td>
                                <td><%= car.car_status %></td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="/editCar/<%= car.car_id %>" class="edit-button">編集</a>
                                        <form action="/deleteCar/<%= car.car_id %>" method="POST" onsubmit="return confirmDelete(event, '<%= car.car_id %>')">
                                            <button type="submit" class="delete-button">削除</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </section>
    </main>
    <%- include('partials/admin-footer') %> 
</body>
</html>
