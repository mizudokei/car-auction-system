<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>入札ページ</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <%- include('partials/admin-header') %>

    <main class="container mx-auto p-4">
        <% if (carDetails) { %>
        <h2 class="text-xl font-semibold mb-4">オークション情報</h2>
        <h2 class="text-xl font-semibold mb-4">オークション終了日:<%= end_datetime %></h2>
        <div class="bg-white p-4 rounded shadow">
            <!-- 車データの表示 -->
                <div class="flex flex-col md:flex-row items-center justify-center text-center space-y-4 md:space-y-0 md:space-x-4">
                    <img src="<%= carDetails.car_image %>" alt="<%= carDetails.car_type %>" class="w-80 h-80 object-cover rounded-lg shadow-md">
                    <table class="table-auto border-collapse">
                        <tbody>
                            <tr>
                                <td class="border px-4 py-2 font-semibold text-2xl">車種</td>
                                <td class="border px-4 py-2"><%= carDetails.car_type %></td>
                            </tr>
                            <tr>
                                <td class="border px-4 py-2 font-semibold text-2xl">メーカー</td>
                                <td class="border px-4 py-2"><%= carDetails.car_manufacturer %></td>
                            </tr>
                            <tr>
                                <td class="border px-4 py-2 font-semibold text-2xl">価格</td>
                                <td class="border px-4 py-2"><%= current_price %>円</td>
                            </tr>
                            <tr>
                                <td class="border px-4 py-2 font-semibold text-2xl">年数</td>
                                <td class="border px-4 py-2"><%= carDetails.car_year %>年</td>
                            </tr>
                            <tr>
                                <td class="border px-4 py-2 font-semibold text-2xl">走行距離</td>
                                <td class="border px-4 py-2"><%= carDetails.car_mileage %> km</td>
                            </tr>
                            <tr>
                                <td class="border px-4 py-2 font-semibold text-2xl">色</td>
                                <td class="border px-4 py-2"><%= carDetails.car_color %></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <form action="/submit-bid" method="get" class="mt-4">
                <div class="mb-4">
                    <label for="bidAmount" class="block text-gray-700">入札額:</label>
                    <input type="number" id="bidAmount" name="bidAmount" class="w-full px-4 py-2 border rounded" min="<%= current_price %>" value="<%= current_price %>" required>
                </div>
                <input type="hidden" name="auction_id" value="<%= auction_id %>">
                <input type="hidden" name="car_id" value="<%= car_id %>">
                <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">入札する</button>
            </form>
            <% } %>
        </main>
    <%- include('partials/admin-footer') %>
</body>
</html>
