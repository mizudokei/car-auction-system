<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <title><%= title %></title>
</head>
<body class="bg-light">
    <%- include('partials/header') %>
    <div class="container my-4">
        <div class="row g-4">
            <% auctionData.forEach(auction => { %>
                <div class="col-sm-12 col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-header bg-primary text-white">
                            <h5 class="card-title mb-0">オークションID: <%= auction.auction_id %></h5>
                        </div>
                        <div class="card-body">
                            <p class="card-text text-muted">開始日時: <%= auction.start_datetime %></p>
                            <p class="card-text text-muted">終了日時: <%= auction.end_datetime %></p>
                            <% if (auction.cars && auction.cars.length > 0) { %>
                                <% auction.cars.forEach(car => { %>
                                    <div class="mb-3 border-bottom pb-3">
                                        <% if (car.car_image) { %>
                                            <img src="<%= car.car_image %>" alt="<%= car.car_type %>" class="img-fluid rounded mb-3">
                                        <% } else { %>
                                            <div class="bg-secondary text-white text-center py-5 rounded mb-3">
                                                画像なし
                                            </div>
                                        <% } %>
                                        <ul class="list-unstyled">
                                            <li><strong>車種:</strong> <%= car.car_type %></li>
                                            <li><strong>メーカー:</strong> <%= car.manufacturer_name %></li>
                                            <li><strong>価格:</strong> <%= car.current_price %>円</li>
                                            <li><strong>年数:</strong> <%= car.car_year %>年</li>
                                            <li><strong>走行距離:</strong> <%= car.car_mileage %></li>
                                            <li><strong>色:</strong> <%= car.car_color %></li>
                                        </ul>
                                        <form action="/bid" method="get" class="text-center mt-3">
                                            <input type="hidden" name="car_id" value="<%= car.car_id %>">
                                            <input type="hidden" name="listing_id" value="<%= car.listing_id %>">
                                            <input type="hidden" name="auction_id" value="<%= auction.auction_id %>">
                                            <input type="hidden" name="end_datetime" value="<%= auction.end_datetime %>">
                                            <button class="btn btn-primary w-100">
                                                入札
                                            </button>
                                        </form>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <p class="text-danger">車情報はありません</p>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
        <% if (auctionData.length === 0) { %>
            <div class="text-center py-5">
                <p class="text-muted fs-4">現在、車情報が登録されていないオークションはありません。</p>
            </div>
        <% } %>
    </div>
    <%- include('partials/footer') %>
</body>
</html>
